---
import MainLayout from '@layouts/MainLayout';
import { getCollection } from 'astro:content';
import FeaturedArticle from '@components/FeaturedArticle';
import { listApiKeys } from '@algolia/client-search';

const posts = (await getCollection('sanitizer'));
const baseURL = `/en/docs/sanitizer`;

const frontmatter = {
	title: `What is safe/unsafe`,
	description: ``,
	sidebarType: `sanitizer`,
}
---

<MainLayout frontmatter={frontmatter}>
	<h2>HTML</h2>

	<h3>Elements</h3>
	<ul>
	{
		posts
		.filter(post => post.slug.startsWith('safe/html/elements'))
		.map((post) => (
			<li>
				<a href={`${baseURL}/${post.slug}`}>
					{post.data.title}
				</a>
			</li>
		))
	}
	</ul>

	<h3>Global Attributes</h3>
	<ul>
	{
		posts
		.filter(post => post.slug.startsWith('safe/html/global-attributes'))
		.map((post) => (
			<li>
				<a href={`${baseURL}/${post.slug}`}>
					{post.data.title}
				</a>
			</li>
		))
	}
	</ul>

	<h3>Other</h3>
	<ul>
	{
		posts
		.filter(post => post.slug.startsWith('safe/html/other'))
		.map((post) => (
			<li>
				<a href={`${baseURL}/${post.slug}`}>
					{post.data.title}
				</a>
			</li>
		))
	}
	</ul>


	<h2>CSS</h2>
	
	<h3>Properties</h3>
	<ul>
	{
		posts
		.filter(post => post.slug.startsWith('safe/css/properties'))
		.map((post) => (
			<li>
				<a href={`${baseURL}/${post.slug}`}>
					{post.data.title}
				</a>
			</li>
		))
	}
	</ul>

	<h3>Values</h3>
	<ul>
	{
		posts
		.filter(post => post.slug.startsWith('safe/css/values'))
		.map((post) => (
			<li>
				<a href={`${baseURL}/${post.slug}`}>
					{post.data.title}
				</a>
			</li>
		))
	}
	</ul>


	<h3>At-rules</h3>
	<ul>
	{
		posts
		.filter(post => post.slug.startsWith('safe/css/at-rules'))
		.map((post) => (
			<li>
				<a href={`${baseURL}/${post.slug}`}>
					{post.data.title}
				</a>
			</li>
		))
	}
	</ul>
	
</MainLayout>