---
let posts = await Astro.glob('../pages/en/reports/email-clients/*.(md|mdx)');
posts = posts
.filter(post => !post.file.endsWith('index.mdx'));
---

<ul>
  {posts.map(post => (
    <li>
      <a href={post.url}>
        {post.frontmatter.title}
      </a>
    </li>
  ))}
</ul>